server:
  port: 8181
bus:
  test: aaaa

eureka:
  instance:
    hostname: order
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: order #服务名
